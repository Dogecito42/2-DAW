/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package Arrays;

/**
 *
 * @author Usuario
 */
public class Ej9 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        /*a) Escriba una instrucción que declare y cree a t. int[] t)=new int[2][3];
            b) ¿Cuantas filas tiene t? 2
c) ¿Cuantas columnas tiene t? 3
d) ¿Cuantos elementos tiene t? 2*3=6
e) Escriba expresiones de acceso para todos los elementos en la fila 1 de t. for (int i=0; i<t.length;i++){ sout(t[1][i]);}
f) Escriba expresiones de acceso para todos los elementos en la columna 2 de t. for (int i=0; i<t.length;i++){ sout(t[i][2]);}
g) Escriba una sola instrucción que asigne cero al elemento de t en la fila 0 y la columna 1. t[0][1]=0;
h) Escriba una serie de instrucciones que inicialice cada elemento de t con cero. No utilice una
instrucción de repetición. Arrays.fill(t[0], 0); Arrays.fill(t[1], 0);
i) Escriba una instrucción for anidada que inicialice cada elemento de t con cero. for (int i = 0; i < t.length; i++) {for (int j = 0; j < t[i].length; j++) {t[i][j] = 0;}}
j) Escriba una instrucción for anidada que reciba como entrada del usuario los valores de los
elementos de t. for (int i = 0; i < t.length; i++) {
    for (int j = 0; j < t[i].length; j++) {System.out.print("Ingrese el valor para t[" + i + "][" + j + "]: ");t[i][j] = scanner.nextInt();}}
k) Escriba una serie de instrucciones que determine e imprima el valor mas pequeño en t. for (int i = 0; i < t.length; i++) {
    for (int j = 0; j < t[i].length; j++) { if (t[i][j] < min) {min = t[i][j];}}}System.out.println("El valor más pequeño en t es: " + min);
l) Escriba una instrucción printf que muestre los elementos de la primera fila de t. No utilice
repetición. System.out.printf("Primera fila de t: %d %d %d%n", t[0][0], t[0][1], t[0][2]);
m) Escriba una instrucción que totalice los elementos de la tercera columna de t. No utilice
repetición. int total = t[0][2] + t[1][2];
System.out.println("Total de la tercera columna: " + total);
n) Escriba una serie de instrucciones para imprimir el contenido de t en formato tabular. Enliste
los indices de columna como encabezados a lo largo de la parte superior, y enliste los
indices de fila a la izquierda de cada fila.*/
    }
    
                   }
