public class ej1 {

    /*
    a) El método static String.format de la clase String es similar al método System.out.printf, pero devuelve un objeto String con formato en vez de mostrar un objeto String en una ventana de comandos.

b) Si un método contiene una variable local con el mismo nombre que uno de los campos de su clase, la variable local oculta al campo en el alcance de ese método.

c) Una declaración import especifica una clase a importar.

d) Si una clase declara constructores, el compilador no creará un(a) constructor predeterminado.

e) El método toString de un objeto se llama en forma implícita cuando aparece un objeto en el código, en donde se necesita un String.

f) A los métodos establecer se les llama comúnmente métodos set o mutadores.

g) Una variable estática representa información a nivel de clase, que comparten todos los objetos de la clase.

h) La palabra clave final especifica que una variable no se puede modificar.

i) A los métodos establecer se les conoce comúnmente como mutadores, ya que, por lo general, modifican un valor.
     */
}