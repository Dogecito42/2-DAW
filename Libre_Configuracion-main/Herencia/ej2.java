public class ej2
{
    /*a) Verdadero. Los constructores de la superclase no son heredados por las subclases. Sin embargo, las subclases pueden invocar los constructores de la superclase mediante la palabra clave super.

b) Falso. Una relación "tiene un" se implementa mediante composición o agregación, no mediante herencia. La herencia describe una relación "es un" (es decir, una subclase es un tipo de la superclase).

c) Falso. Una clase Auto tiene una relación "tiene un" con las clases VolanteDireccion y Frenos, ya que un auto tiene volante y frenos, no es un volante o freno.

d) Verdadero. La herencia fomenta la reutilización de software comprobado, de alta calidad, al permitir que las subclases hereden las funcionalidades de las superclases sin necesidad de reescribir el código.

e) Falso. Cuando una subclase redefine un método de la superclase utilizando la misma firma, se dice que la subclase sobreescribe (no sobrecarga) el método de la superclase. La sobrecarga ocurre cuando hay múltiples métodos con el mismo nombre pero diferentes firmas (diferentes parámetros).

f) Verdadero. Es posible tratar a los objetos de superclase y a los objetos de subclase de manera similar, ya que los objetos de la subclase pueden ser tratados como objetos de la superclase debido a la relación de herencia.

g) Falso. No todos los métodos en una clase abstracta deben declararse como métodos abstractos. Una clase abstracta puede tener tanto métodos abstractos (sin implementación) como métodos concretos (con implementación).

h) Falso. Es peligroso invocar a un método que solo pertenece a una subclase a través de una variable de superclase si la referencia de la superclase no apunta realmente a una instancia de la subclase. Esto puede causar problemas si el método no está definido en la superclase.

i) Verdadero. Si una superclase declara un método como abstracto, una subclase debe implementar ese método, a menos que la subclase también se declare como abstracta.*/

}
